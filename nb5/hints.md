# Notebook 5 — hints

- The pooled fit throws every trial into one cloud. Is there a grouping variable
  in the data (session) that is related to *both* pupil and firing rate?

- Re-draw the scatter, coloring points by session. What is the slope *within*
  each session? How does that compare to the pooled slope?

- "Do high-arousal sessions fire more?" and "does arousal drive firing within a
  session?" are different questions. Which one does the pooled correlation
  actually answer?

- How could you estimate the within-session relationship (e.g. remove each
  session's mean from both variables before pooling, or fit a model with a
  per-session intercept)?
